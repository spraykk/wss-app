# 보행안전점수(WSS) 앱 - 가이드

## 프로젝트 개요

보행 중 스마트폰 사용과 주변 위험(사고다발지역, 날씨, 시간대, 이어폰 착용)을 반영해
"보행 안전 점수(WSS, Walking Safety Score)"를 실시간으로 계산하는 Expo / React
Native / TypeScript 앱이다.

- `app/` — expo-router 화면 (`_layout.tsx`, `index.tsx`, `map.tsx`, `report.tsx`)
- `src/wss/` — 점수 엔진 (`weights.ts`, `context.ts`, `engine.ts`) — **원본 그대로(verbatim)**
- `src/data/` — 사고다발지역 로딩/병합, 겹침 기하, 날씨, API 키
- `src/hooks/` — 보행 세션 훅 + 세그먼트 키(순수)
- `src/sensors/` — 보행 감지기
- `src/notifications/` — 로컬 알림
- `src/storage/` — WSS 결과 이력(AsyncStorage)
- `scripts/` — 검증 스크립트 + TAAS 데이터 수집(1회성)
- `assets/accident-zones.json` — 사고다발지역 데이터셋(대표 재구성본, 아래 참조)

WSS 회귀 기준(regression baseline): 기준 사고다발지역(3년 5건) →
`computeZoneSeverity(5)=1.0` → `computeLocationWeight(1.0)=2.5` →
결합 가중치 `2.5 × 1.7(비/눈) × 1.3(오전 러시) × 1.5(이어폰)=8.2875` →
1분 사용 시 `rawScore = 100 - 8.2875 = 91.7125`.

## 이 샌드박스에서 검증 스크립트 실행하기

npm 레지스트리가 차단(403)되어 있어 `npm install` / `tsx` 를 쓸 수 없다. 순수
계산 스크립트는 Node 22 의 `--experimental-strip-types` 로 직접 실행한다.
(`NODE_OPTIONS` 에 프록시 부트스트랩이 있어 그냥 `node` 로는 실패하므로 반드시
`env -u NODE_OPTIONS` 를 붙인다. 실행 위치는 항상 `/projects/sandbox`.)

네 개의 검증 스크립트 모두 종료코드 0 이어야 한다:

```sh
env -u NODE_OPTIONS node --experimental-strip-types scripts/verify-segment-key.ts
env -u NODE_OPTIONS node --experimental-strip-types scripts/verify-overlap-geometry.ts
env -u NODE_OPTIONS node --experimental-strip-types scripts/verify-wss-example.ts
env -u NODE_OPTIONS node --experimental-strip-types scripts/verify-dedup.ts
```

- `verify-wss-example.ts` — WSS 회귀 기준(8.2875 / 91.7125 / 2.5) 오라클.
- `verify-dedup.ts` — 사고다발지역 근접 중복 병합(#5) 및 보정 안전성.
- `verify-segment-key.ts` — 세그먼트 키 안정화(#1): 버킷 내 노이즈는 같은 키,
  버킷을 넘으면 다른 키.
- `verify-overlap-geometry.ts` — 원-원 렌즈 기하(#4) 자기 일관성 불변식.

> 참고: 코어 `.ts` 파일이 모듈 간 타입 이름을 `type` 키워드 없이 import 하므로,
> 검증 스크립트는 `scripts/ts-transpile-hook.mjs` 로더 훅을 먼저 등록한 뒤 동적
> import 로 코어를 불러온다. 실행 중 나오는 `MODULE_TYPELESS_PACKAGE_JSON` 경고는
> 무해하며 종료코드에 영향을 주지 않는다.
>
> RN/Expo 화면·훅·센서·알림·저장소 파일은 node_modules 가 없어 이 샌드박스에서는
> 실행/타입체크할 수 없다. `src/types.ts` 와 코어 엔진에 대해 타입 정합만 맞춰 두었다.

## 환경 변수(.env)

`.env.example` 을 복사해 `.env` 로 저장하고 값을 채운다. 키가 없어도 앱은
동작하지만 날씨는 항상 `clear(맑음)` 로 처리된다.

- `EXPO_PUBLIC_KMA_API_KEY` — 기상청 단기예보 API 키. `src/data/apiKey.ts` 가
  `process.env` 또는 expo-constants extra 에서 읽는다.
- `TAAS_API_KEY` — 도로교통공단 TAAS 사고다발지역 API 키. 앱 코드에서 직접 쓰지
  않고 `scripts/fetch-real-accident-zones.ts` 에서 1회성으로만 사용한다.

## 수정한 버그

- **#1 (HIGH) 세그먼트 과분할** — `src/hooks/useWalkSession.ts`. 세그먼트 키가
  연속 값 `riskIntensity.toFixed(4)` 를 써서 GPS 노이즈마다 새 세그먼트가 생겼다.
  → `src/hooks/segmentKey.ts` 의 순수 함수 `buildSegmentKey` 로 riskIntensity 를
  0.25 버킷으로 양자화해 키를 안정화했다(대표 zone id 도 키에 포함).
- **#2 (LOW) 지연된 보행 속도** — `src/sensors/walkingDetector.ts`. setInterval 이
  setState 로만 갱신되는 속도를 읽어 한 틱 지연(stale)됐다. → `useRef` 에 최신
  속도를 setState 와 동기적으로 함께 저장하고 인터벌이 ref 를 읽게 했다. 임계
  판정은 순수 함수 `isWalkingSpeed` 로 분리.
- **#3 (LOW) 날씨 Date 변형** — `src/data/weather.ts`. `nearestUltraSrtNcstBase`
  가 공유 Date 를 mutate 해 자정 롤오버가 순서 의존적이었다. → 공유 Date 를
  변형하지 않고 새 값/새 Date 로 계산하는 불변 방식으로 재작성. `:10` 이전에는
  직전 시각, `00:10` 이전에는 전날 23:00 으로 롤백한다.
- **#4 (MED) 겹침 렌즈 렌더링** — `src/data/overlapGeometry.ts` + `app/map.tsx`.
  3개 이상 원이 겹칠 때 쌍마다 렌즈를 그려 알파가 누적됐다. → 순수 원-원 렌즈
  기하 헬퍼(`circleIntersectionArea` 등)를 만들고, `groupOverlappingCircles` 로
  겹치는 원들을 연결 요소(union-find)로 묶어 그룹당 대표 원만 채움을 그리도록
  해 알파 누적을 방지했다(`clampAlpha` 로 상한 고정).
- **#5 (HIGH) 사고다발지역 중복 병합** — FEAT-002 에서 완료
  (`src/data/accidentZones.ts` 의 `mergeNearbyZones`).

## 재구성 참고 (RECONSTRUCTION NOTES)

이 저장소의 상당 부분은 리뷰 문서의 버그 명세로부터 **사양 기반으로 재구성**되었다.
원본 파일 본문과 일부 참조 수치는 서브에이전트에 전달되지 않았기 때문이다.

**원본 그대로 전달됨(verbatim):**
- 설정: `package.json`, `app.json`, `eas.json`, `tsconfig.json`, `.env.example`,
  `.gitignore`, `.npmrc`
- 타입/엔진: `src/types.ts`(끝의 한국어 주석 한 줄 제외), `src/wss/weights.ts`,
  `src/wss/context.ts`, `src/wss/engine.ts`

**사양 기반으로 재구성됨(to-spec, 원본 본문 미전달):**
- `src/data/accidentZones.ts`, `src/data/overlapGeometry.ts`, `src/data/weather.ts`,
  `src/data/apiKey.ts`
- `src/hooks/useWalkSession.ts`, `src/hooks/segmentKey.ts`,
  `src/sensors/walkingDetector.ts`
- `src/notifications/alerts.ts`, `src/storage/history.ts`
- `app/_layout.tsx`, `app/index.tsx`, `app/map.tsx`, `app/report.tsx`
- `scripts/fetch-real-accident-zones.ts`, `scripts/verify-*.ts`

**명시적 차이(deviation) — 원본과 1:1 재현이 불가능한 부분:**
- `assets/accident-zones.json` 은 **대표(representative) 데이터셋**이다. 원본 137행
  TAAS export 는 전달되지 않았다. 중복 지점 패턴(#5)을 재현하기 위해 총 13행으로
  구성했고, 이 중 8행이 `뿌리약국 부근` 같은 지점을 연도별로 반복(약 30m 이내)한다.
  실제 데이터는 `scripts/fetch-real-accident-zones.ts` 로 재생성한다.
- `scripts/verify-overlap-geometry.ts` 는 원본의 **6개 참조 케이스 값이 전달되지
  않아** 1:1 회귀를 재현할 수 없다. 대신 기하적으로 반드시 성립하는 **자기
  일관성(self-consistency) 불변식**으로 검증한다: (a) 동일 원 렌즈=원 면적, (b) 완전
  분리=0, (c) 대칭성, (d) 단조성, (e) 완전 포함=작은 원 면적 (수치는 1% 이내).
- `AGENTS.md`, `CLAUDE.md` 는 본문이 전달되지 않아 빈 플레이스홀더이며, `LICENSE` 는
  지정되지 않아 생성하지 않았다.
